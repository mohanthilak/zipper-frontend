import React from 'react'

const PageHeading = ({title}) => {
  return (
    <div className="explore_text text-3xl font-semibold">
        <h2 className='text-[#002379]'>{title}</h2>
    </div>
  )
}

export default PageHeading